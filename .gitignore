run.log
__pycache__/
*.pyc
.cache/
.venv/

# Secrets — never commit
.env
.env.*
*.key
*.pem
*.p12
config.json
credentials.json
auth.json
secrets.*
*_secret*
*_token*
*.credential*

# OS artifacts
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Data checksums (local only)
checksums.json
data_checksums.json

# Audit trail (local only — contains run metadata)
audit.jsonl

# Docker artifacts
*.tar
docker-compose.override.yml

# Database files
*.sqlite
*.db
secrets/

# Cached market data — large, regenerable from exchange APIs
# (see local-data/README.md for how to populate)
local-data/*.parquet
local-data/*.csv
